# This file controls the jlink'ed modules that are included in the fusionauth-app
# Docker image and explains why they are here
# To add or remove a module, edit the list below and rebuild the Docker image.
java.base
java.compiler
# We need the java.beans.Introspector class for Prime MVC
java.desktop
# Allow agents (e.g. APM monitoring, etc.)
java.instrument
java.logging
java.management
java.naming
java.net.http
java.security.sasl
java.sql
# java.xml is just a transitive dependency of java.xml.crypto
java.xml
java.xml.crypto
# dynamic agent loading, tool integration
jdk.attach
jdk.crypto.ec
jdk.dynalink
# jstack/thread dumps, etc.
jdk.jcmd
# JPDA debugging
jdk.jdi
jdk.localedata
# some libraries like Jackson, etc. depend on this. sun.misc.Unsafe, sun.misc.Signal
jdk.unsupported
# ZIP filesystem/compression
jdk.zipfs
